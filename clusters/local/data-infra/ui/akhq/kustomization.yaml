apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
- helmrelease.yaml
configMapGenerator:
- name: akhq-values
  files:
  - values.yaml
generatorOptions:
  disableNameSuffixHash: true
