apiVersion: v1
kind: Service
metadata:
  name: dp-postgres
  namespace: data-infra
spec:
  selector:
    app.kubernetes.io/name: nessie-postgres
    app.kubernetes.io/component: metastore
    app.kubernetes.io/instance: dp
  ports:
  - name: postgres-endpoint
    protocol: TCP
    port: 5432
    targetPort: 5432
